{
  "name": "@aise/backend-api",
  "version": "0.0.0",
  "private": true,
  "description": "AISE HTTP API service boundary (foundation: health, lifecycle, request logging)",
  "type": "module",
  "dependencies": {
    "@aise/backend-config": "*",
    "@aise/backend-logging": "*",
    "@aise/shared-contracts": "*",
    "ajv": "^8.17.1"
  },
  "scripts": {
    "dev": "tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
