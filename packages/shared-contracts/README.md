# @aise/shared-contracts — RESERVED

This package is **reserved for AISE-003 — Shared contract package**
(SHARED Work Item, primary agent ZAI, secondary agent GEMINI).

AISE-001 deliberately adds **no code here**:

- Contract content and versioning are defined by the AISE-003 Work Order.
- Shared contract surfaces must not be created ad hoc by either agent
  (`spec/agent-ownership.md`, shared contract protocol).
- Until AISE-003 is merged, this directory exists only to reserve the
  workspace location declared by the architecture surface map.
